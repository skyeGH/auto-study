# 刷课工具

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `开始刷课.bat` | **双击运行** 启动刷课 |
| `study.js` | 刷课主程序（无需修改） |
| `config.json` | **配置文件**（需要修改） |
| `fetch解析工具.html` | **解析工具**（无需修改） |

---

## 🚀 使用方法

### 第一步 安装node
在压缩包里有一个node-v24.15.0-x64.msi的文件，这个文件是用来运行js程序的，直接默认安装就行

### 第二步解析fetch
1、打开fetch解析工具.html
2、粘贴并解析fetch
3、将生成的config.json复制并直接覆盖`config.json`文件内容

（将 fetch 解析工具生成的内容覆盖到 config.json 文件中）

### 第三步：双击运行

双击 `开始刷课.bat`，程序会自动：
1. 检查 Node.js 环境
2. 读取配置文件
3. 开始自动刷课
4. 显示实时进度

### 第四步：暂停视频就不用管了，把命令提示框最小化让他跑就行了

（程序运行后命令行会显示实时进度，将窗口最小化让其在后台运行即可）

这是正常页面

---

## 📋 如何获取配置信息

1. 浏览器打开学习页面，登录账号
2. 按 **F12** 打开开发者工具
3. 切换到 **Network（网络）** 标签
4. 点击任意视频，观看十几秒**出现的第二个cv.gson ** 
5. 找到 `cv.gson` 请求，右键点击它
右键点击 cv.gson 请求，选择"复制 → 复制为 fetch(Node.js)"



---

## ⚙️ 配置参数说明（默认值无需修改）

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `cookie` | 登录凭证 | 必填 |
| `myClassId` | 班级ID | 必填 |
| `myClassCourseId` | 课程ID | 必填 |
| `videos` | 视频列表 | 必填 |
| `delay` | 请求间隔（毫秒） | 5000 |
| `segmentSize` | 每次上报时长（秒） | 300 |
| `targetProgress` | 目标进度（%） | 100 |

---

---

## ⚠️ 注意事项

1. **Cookie 会过期** - 如果提示未登录，需要重新复制 Cookie
2. **不要频繁运行** - 建议每次运行间隔几分钟
3. **duration 要够大** - 建议比实际视频时长多 200-500 秒

---

## 🐛 常见问题

### Q: 提示 "非法的参数!(time)"
A: 上报的时长超过了视频实际时长，减小 `duration` 值

### Q: 进度一直不涨
A: Cookie 可能过期了，重新从浏览器复制最新的 Cookie

---

